/*
package br.com.zup.bytebank

import org.junit.Assert
import org.junit.Test

class ContaTest {

    @Test
    private fun testaDepositoConta() {
        val conta = ContaPoupanca("Rafael", "0001", 10000.0)
        conta.deposita(2000.0)
        Assert.assertEquals(12000.0, conta.saldo, 0.0001)
    }

    @Test
    private fun testaSaqueConta() {
        val conta = ContaPoupanca("Rafael", "0001", 10000.0)
        conta.saca(2000.0)
        Assert.assertEquals(8000.0, conta.saldo, 0.0001)
    }

    @Test
    private fun testaTransferenciaConta() {
        val conta = ContaPoupanca("Rafael", "0001", 10000.0)
        val conta2 = ContaPoupanca("Rafael Neto", "0011", 1000.0)
        conta2.transferencia(conta, 1000.0)
        Assert.assertEquals(0.0, conta2.saldo, 0.0001)
        Assert.assertEquals(11000.0, conta.saldo, 0.0001)
    }

}*/
